import io.kotest.core.spec.style.FunSpec

class TempStub: FunSpec({
    // TODO delete this class and replace with real tests
    //  this is here to prevent the build from failing with an empty test state
    //  since there is a test folder to house the petstore samples
})
