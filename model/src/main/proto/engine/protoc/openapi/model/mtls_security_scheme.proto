syntax = "proto3";

package engine.protoc.openapi.model;

// Mutual TLS security scheme. Added in OpenAPI 3.1.0 (not present in 3.0).
//
// This scheme type requires no additional configuration fields beyond the scheme `type`; the
// client certificate is negotiated at the TLS layer rather than at the HTTP application layer.
// A description MAY be added to the parent [SecurityScheme](https://swagger.io/specification/#security-scheme-object).
message MutualTLSSecurityScheme {
}
