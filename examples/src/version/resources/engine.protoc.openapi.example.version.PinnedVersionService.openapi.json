{
  "openapi" : "3.1.0",
  "paths" : {
    "/DoIt" : {
      "get" : {
        "summary" : "Gets a widget by its identifier.",
        "description" : "Gets a widget by its identifier.",
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        }
      }
    }
  },
  "info" : {
    "title" : "PinnedVersionService",
    "description" : "A service whose API version is pinned by an explicit service-level engine annotation.\n Regardless of what options.version is set to, the annotation version always wins.",
    "version" : "pinned-1.0.0"
  }
}
